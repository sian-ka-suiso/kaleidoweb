RewriteEngine On

# .php拡張子なしのURLでアクセスされた場合、実際の.phpファイルを返す
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^([^.]+)$ $1.php [L]

# .php拡張子付きでアクセスされたら拡張子なしURLへリダイレクト(SEO対策・重複URL防止)
RewriteCond %{THE_REQUEST} ^[A-Z]{3,}\s([^.]+)\.php[\s?] [NC]
RewriteRule ^ %1 [R=301,L]